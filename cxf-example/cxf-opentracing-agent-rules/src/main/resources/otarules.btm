RULE CXF Enable Client Tracing Feature
CLASS org.apache.cxf.jaxrs.client.JAXRSClientFactoryBean
METHOD create
HELPER io.opentracing.contrib.agent.OpenTracingHelper
AT ENTRY
BIND
  feature : org.apache.cxf.feature.Feature = new org.apache.cxf.tracing.opentracing.OpenTracingClientFeature(getTracer());
IF TRUE
DO
  $this.getFeatures().add(feature);
ENDRULE

RULE CXF Enable Server Tracing Feature
CLASS org.apache.cxf.jaxrs.JAXRSServerFactoryBean
METHOD create
HELPER io.opentracing.contrib.agent.OpenTracingHelper
AT ENTRY
BIND
  feature : org.apache.cxf.feature.Feature = new org.apache.cxf.tracing.opentracing.OpenTracingFeature(getTracer());
IF TRUE
DO
  $this.getFeatures().add(feature);
ENDRULE
